# Knowledge base

Markdown under **`knowledge/`** is indexed for workspace search when the indexer or GitHub `push` webhook runs. See the EDF docs (`knowledge_repo_and_index.md` in the framework repo).

**Layout:** use **`knowledge/business/`** for customer-facing articles and **`knowledge/technical/`** for internal-only docs (default visibility rules apply in the app).
